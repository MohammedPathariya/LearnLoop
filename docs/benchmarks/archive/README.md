# Archived retrieval benchmarks

These exploratory tests are retained for provenance but are excluded from the active benchmark comparison:

- `retrieval_dataset.json` and `retrieval_results.json`: v1, 5 synthetic chunks, Recall@5.
- `retrieval_dataset_v2.json` and `retrieval_results_v2.json`: v2, 25 synthetic chunks with distractors, Recall@5.
